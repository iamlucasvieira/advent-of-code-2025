"""Tests for day {day}."""

from helpers import load_file

from advent_of_code_2020 import day{day_padded}


def test_parse_input():
    """Test parsing the input data."""
    input_data = load_file(day={day}, part=None, is_example=True)
    parsed = day{day_padded}.parse_input(input_data)
    assert parsed is not None


def test_part1_example():
    """Test part 1 with example input."""
    input_data = load_file(day={day}, part=None, is_example=True)
    result = day{day_padded}.part1(input_data)
    # TODO: Update with expected result from puzzle
    assert result == 0


def test_part2_example():
    """Test part 2 with example input."""
    input_data = load_file(day={day}, part=None, is_example=True)
    result = day{day_padded}.part2(input_data)
    # TODO: Update with expected result from puzzle
    assert result == 0
